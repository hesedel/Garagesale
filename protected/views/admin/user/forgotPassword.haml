:php
	$this->pageTitle=Yii::app()->name . ' - Forgot Password';

	$this->layout = '/layouts/transition';

#user_forgotPassword
	%h2.alert Password change email sent!
	%p.alert.alert-info An email has been sent to you ( <strong>#{$email}</strong> )<br/>Please follow the instructions in the email to change your password.<br/>If you can't see the email, it might be in your spam folder.
	=CHtml::link('← return to where you left off', Yii::app()->user->getReturnUrl(), array('class'=>'g-button small'))