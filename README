NOTE

For development environment specific settings to apply,
	make sure your url starts with 'localhost.'.

	ex:
		http://localhost.garagesale.com/


INSTRUCTIONS

1. Create a database and import either 'garagesale.sql' for a blank database or 'garagesale-populated.sql' for a pre-populated one.

2. Duplicate '/protected/config/local.php.sample' to '/protected/config/local.php'
	then edit it to match your local environment configuration.

3. FOR MAC USERS:
	Run '$ bash mac'.